import { Typography, styled } from "@mui/material"

export const CustomMenuTypography = styled(Typography)({
    fontWeight: 600,
    fontSize: "0.8rem",
    color: "#171819",
    textAlign: "center",
    alignItems: "center",
    display: "flex"
  })

export const CustomMenuItemTypography = styled(Typography)({
    fontWeight: 500,
    fontSize: "0.8rem",
    color: "#171819",
    textAlign: "center",
    alignItems: "center",
    display: "flex"
  })

export const CustomHeadingTypography = styled(Typography)({
  fontWeight: 700,
  fontSize: "2rem",
  color: "#1F284F",
})

export const CustomDiscriptionTypography = styled(Typography)({
  fontWeight: 400,
  fontSize: "1.25rem",
  color: "#575757",
  marginTop: "1rem",
  paddingBottom: "2rem"
})

export const CustomFormLabel = styled(Typography)({
  fontWeight: 500,
  fontSize: "0.8rem",
  color: "#515458",
})